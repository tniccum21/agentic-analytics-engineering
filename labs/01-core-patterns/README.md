# Module 1 quick start

```bash
cd labs/01-core-patterns
python3 -m venv .venv
source .venv/bin/activate
pip install pytest
pytest -q
```

Start with `patterns.py`; the supplied tests define the required control-flow behavior.
